import crypto from'node:crypto';
const b64=b=>b.toString('base64url');
export async function GET(r){const state=b64(crypto.randomBytes(18)),verifier=b64(crypto.randomBytes(48)),challenge=b64(crypto.createHash('sha256').update(verifier).digest()),origin=new URL(r.url).origin,u=new URL('https://accounts.spotify.com/authorize');u.search=new URLSearchParams({client_id:process.env.SPOTIFY_CLIENT_ID,response_type:'code',redirect_uri:`${origin}/api/spotify/callback`,scope:'playlist-read-private playlist-modify-private playlist-modify-public',code_challenge_method:'S256',code_challenge:challenge,state,show_dialog:'true'});return new Response(null,{status:302,headers:{location:u.toString(),'set-cookie':`spotify_oauth=${state}.${verifier}; HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=600`}})}
